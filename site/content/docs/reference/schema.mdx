---
title: Schema
description: Every table Entl writes — generated from the fluessig catalog.
---

{/* AUTO-GENERATED from crates/fluessig/schema_docs.json (lowered from crates/fluessig/entl.tsp) by scripts/gen-reference.ts — run `bun run gen`. Do not edit by hand. */}

# Schema

Entl writes one DuckDB. **git-generic** tables are bare (so a future forge reuses them);
**GitHub** tables are namespaced `gh_*`. 28 tables in total.

<Callout>
Generated from the SQL migrations. Diffs and blob contents are **not** stored — they're
computed on demand (`diffCommits`, `fileAt`; see the [Node API](/docs/reference/node-api)).
</Callout>

## git

### `blobs`

| Column | Type | Description | |
|---|---|---|---|
| `oid` | `blob` | object id — the content hash (binary; hex via `lower(hex(oid))`). Globally unique, so the family key is oid alone. | **PK** |
| `repo_id` | `text` | the repo this object was seen in (an association, not a key member) | not null |
| `size` | `bigint` |  | not null |
| `is_binary` | `boolean` |  | not null, default `false` |
| `content_text` | `text` |  |  |
| `content_sha` | `text` |  |  |
| `content` | `blob` | raw file bytes (object ingest / --objects); enables `entl rebuild` |  |

### `commit_parents`

this commit's parents, ordered by `idx` (merges have several)

| Column | Type | |
|---|---|---|
| `commit_oid` | `blob` | **PK** |
| `idx` | `integer` | **PK** |
| `parent_oid` | `blob` | not null |

### `commits`

One row per commit, walked from every ref. Author and committer time/identity
are kept separate; `summary` is the first line of `message`.

| Column | Type | Description | |
|---|---|---|---|
| `oid` | `blob` | object id — the content hash (binary; hex via `lower(hex(oid))`). Globally unique, so the family key is oid alone. | **PK** |
| `repo_id` | `text` | the repo this object was seen in (an association, not a key member) | not null |
| `tree_oid` | `blob` | root tree this commit points at | not null |
| `message` | `text` | full commit message | not null |
| `summary` | `text` | first line of the message | not null |
| `author_name` | `text` |  |  |
| `author_email` | `text` |  |  |
| `author_when` | `timestamp` |  |  |
| `author_tz` | `text` |  |  |
| `committer_name` | `text` |  |  |
| `committer_email` | `text` |  |  |
| `committer_when` | `timestamp` |  |  |
| `committer_tz` | `text` |  |  |
| `parent_count` | `integer` |  | not null, default `0` |
| `is_merge` | `boolean` | The §9.3 v1 carve-out: a REAL derived field — true iff a second parent
exists. Today it is ALSO a stored column (the source computes it; parity
with the hand-written templates) and the codec additionally emits a
`commits_derived` view computing it from `parents`. At the Step-5 swap the
stored column drops and the view becomes the only projection. | not null, default `false` |
| `gpg_signed` | `boolean` |  | not null, default `false` |

### `conflicts`

Merge-conflict hot zones (the north star). Populated by the `entl conflicts`
pass: replay every historical 2-parent merge with gix's 3-way tree merge and
record the paths that conflicted. `unresolved` = git would have required a
human, vs auto-resolved.

| Column | Type | Description | |
|---|---|---|---|
| `repo_id` | `text` |  | **PK** |
| `merge_oid` | `text` | hex text today, while every other oid column is blob — schema inconsistency, FINDINGS #8 | **PK** |
| `path` | `text` |  | **PK** |
| `unresolved` | `boolean` |  | not null |

### `file_changes`

Per-commit file changes — a WEAK ENTITY: identified by (owner, path), no
global id of its own. Authored as an entity with FK-in-PK; whether it should
*also* be a

| Column | Type | |
|---|---|---|
| `commit_oid` | `blob` | **PK** |
| `path` | `text` | **PK** |
| `old_path` | `text` |  |
| `status` | `text` | not null |
| `additions` | `integer` |  |
| `deletions` | `integer` |  |
| `blob_oid` | `blob` |  |
| `old_blob_oid` | `blob` |  |

### `refs`

Branches, tags, remote-tracking refs, and HEAD — one row each.

| Column | Type | Description | |
|---|---|---|---|
| `repo_id` | `text` |  | **PK** |
| `name` | `text` | short ref name, e.g. `main` or `origin/main` | **PK** |
| `kind` | `text` |  | not null |
| `target_oid` | `blob` | Today: implicitly commit-typed (no discriminator column). The honest model
is `target: GitObject` — an annotated-tag ref targets a tag object, not a
commit — but that adds a target_type column, i.e. a real schema change.
Authored as Commit for byte parity; the upgrade is FINDINGS #7. | not null |
| `is_symbolic` | `boolean` |  | not null, default `false` |
| `upstream` | `text` |  |  |

### `repos`

A repository entl has synced.

| Column | Type | |
|---|---|---|
| `id` | `text` | **PK** |
| `path` | `text` | not null |
| `remote_url` | `text` |  |
| `host` | `text` |  |
| `owner` | `text` |  |
| `name` | `text` |  |
| `default_branch` | `text` |  |
| `first_synced_at` | `timestamp` |  |
| `last_synced_at` | `timestamp` |  |

### `sync_state`

Per-resource sync bookkeeping (etags, cursors, watermarks).

| Column | Type | |
|---|---|---|
| `resource` | `text` | **PK** |
| `cursor` | `text` |  |
| `etag` | `text` |  |
| `watermark` | `timestamp` |  |
| `last_synced_at` | `timestamp` |  |
| `last_error` | `text` |  |

### `tree_entries`

The polymorphic edge already in production: tree_entries stores the
(type, key) pair as (entry_type, child_oid) — exactly DESIGN §2's
projection of a polymorphic association.

| Column | Type | |
|---|---|---|
| `tree_oid` | `blob` | **PK** |
| `name` | `text` | **PK** |
| `path` | `text` | not null |
| `mode` | `text` | not null |
| `entry_type` | `text` | not null |
| `child_oid` | `blob` | not null |

### `trees`

| Column | Type | Description | |
|---|---|---|---|
| `oid` | `blob` | object id — the content hash (binary; hex via `lower(hex(oid))`). Globally unique, so the family key is oid alone. | **PK** |
| `repo_id` | `text` | the repo this object was seen in (an association, not a key member) | not null |


## GitHub

### `gh_assignees`

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `subject_type` | `text` | **PK** |
| `subject_number` | `integer` | **PK** |
| `user_id` | `bigint` | **PK** |

### `gh_check_runs`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `repo_id` | `text` | not null |
| `commit_oid` | `blob` |  |
| `name` | `text` |  |
| `status` | `text` |  |
| `conclusion` | `text` |  |
| `started_at` | `timestamp` |  |
| `completed_at` | `timestamp` |  |

### `gh_comments`

Issue/PR comments — subject is a polymorphic reference into the GhSubject family.

| Column | Type | Description | |
|---|---|---|---|
| `id` | `bigint` |  | **PK** |
| `subject_type` | `text` |  | not null |
| `repo_id` | `text` | the (type, key) pair: (subject_type, repo_id + subject_number) | not null |
| `subject_number` | `integer` | the (type, key) pair: (subject_type, repo_id + subject_number) | not null |
| `author_id` | `bigint` |  |  |
| `body` | `text` |  |  |
| `created_at` | `timestamp` |  |  |

### `gh_commit_statuses`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `repo_id` | `text` | not null |
| `commit_oid` | `blob` | not null |
| `context` | `text` |  |
| `state` | `text` |  |
| `description` | `text` |  |
| `target_url` | `text` |  |
| `created_at` | `timestamp` |  |

### `gh_events`

Raw GitHub event stream (the /events feed) — entl's "did anything happen?"
signal AND a queryable activity log. The feed is capped (~300 events / 90
days): complete going FORWARD from when polling starts.

| Column | Type | Description | |
|---|---|---|---|
| `repo_id` | `text` |  | **PK** |
| `id` | `text` |  | **PK** |
| `type` | `text` |  |  |
| `actor_id` | `bigint` | denormalized pair: the association + a plain login column (FINDINGS #9) |  |
| `actor_login` | `text` |  |  |
| `created_at` | `timestamp` |  |  |
| `payload` | `text` | the event's type-specific JSON |  |

### `gh_issues`

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `number` | `integer` | **PK** |
| `title` | `text` |  |
| `body` | `text` |  |
| `state` | `text` | not null |
| `author_id` | `bigint` |  |
| `created_at` | `timestamp` |  |
| `updated_at` | `timestamp` |  |
| `closed_at` | `timestamp` |  |

### `gh_jobs`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `run_id` | `bigint` | not null |
| `name` | `text` |  |
| `status` | `text` |  |
| `conclusion` | `text` |  |
| `started_at` | `timestamp` |  |
| `completed_at` | `timestamp` |  |
| `runner_name` | `text` |  |

### `gh_labeled`

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `subject_type` | `text` | **PK** |
| `subject_number` | `integer` | **PK** |
| `label_name` | `text` | **PK** |

### `gh_labels`

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `name` | `text` | **PK** |
| `color` | `text` |  |
| `description` | `text` |  |

### `gh_pr_commits`

plain join table (PK = all columns): the PR's commits

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `pr_number` | `integer` | **PK** |
| `commit_oid` | `blob` | **PK** |

### `gh_pr_reviews`

PR reviews — globally id-keyed, referencing the PR by its composite key.

| Column | Type | Description | |
|---|---|---|---|
| `id` | `bigint` |  | **PK** |
| `repo_id` | `text` | multi-column FK to a composite-keyed entity (repo_id, pr_number) | not null |
| `pr_number` | `integer` | multi-column FK to a composite-keyed entity (repo_id, pr_number) | not null |
| `reviewer_id` | `bigint` |  |  |
| `state` | `text` |  |  |
| `submitted_at` | `timestamp` |  |  |
| `body` | `text` |  |  |

### `gh_pull_requests`

Pull requests and their lifecycle. `mergeable` + `checks` are the live
conflict and CI signals; head/base oids drive on-demand base...head diffs.

| Column | Type | Description | |
|---|---|---|---|
| `repo_id` | `text` |  | **PK** |
| `number` | `integer` |  | **PK** |
| `title` | `text` |  |  |
| `body` | `text` |  |  |
| `state` | `text` |  | not null |
| `author_id` | `bigint` |  |  |
| `created_at` | `timestamp` |  |  |
| `updated_at` | `timestamp` |  |  |
| `closed_at` | `timestamp` |  |  |
| `merged_at` | `timestamp` |  |  |
| `merge_commit_oid` | `blob` |  |  |
| `head_ref` | `text` |  |  |
| `base_ref` | `text` |  |  |
| `additions` | `integer` |  |  |
| `deletions` | `integer` |  |  |
| `changed_files` | `integer` |  |  |
| `is_draft` | `boolean` |  | not null, default `false` |
| `mergeable` | `text` |  |  |
| `checks` | `text` | head commit CI rollup: SUCCESS \| FAILURE \| PENDING \| … (open set — stays text) |  |
| `head_oid` | `blob` | PR head commit (for base...head diffs) |  |
| `base_oid` | `blob` | base branch tip at fetch time |  |

### `gh_requested_reviewers`

plain join table: requested reviewers

| Column | Type | |
|---|---|---|
| `repo_id` | `text` | **PK** |
| `pr_number` | `integer` | **PK** |
| `user_id` | `bigint` | **PK** |

### `gh_review_comments`

| Column | Type | Description | |
|---|---|---|---|
| `id` | `bigint` |  | **PK** |
| `repo_id` | `text` |  | not null |
| `pr_number` | `integer` |  | not null |
| `path` | `text` |  |  |
| `line` | `integer` |  |  |
| `side` | `text` |  |  |
| `commit_oid` | `blob` |  |  |
| `author_id` | `bigint` |  |  |
| `body` | `text` |  |  |
| `created_at` | `timestamp` |  |  |
| `in_reply_to` | `bigint` | threaded replies — a nullable self-association |  |

### `gh_steps`

Weak entity again: a step is (job, number), no global id.

| Column | Type | |
|---|---|---|
| `job_id` | `bigint` | **PK** |
| `number` | `integer` | **PK** |
| `name` | `text` |  |
| `status` | `text` |  |
| `conclusion` | `text` |  |
| `started_at` | `timestamp` |  |
| `completed_at` | `timestamp` |  |

### `gh_users`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `login` | `text` | not null |
| `type` | `text` |  |
| `name` | `text` |  |

### `gh_workflow_runs`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `repo_id` | `text` | not null |
| `workflow_id` | `bigint` |  |
| `head_oid` | `blob` |  |
| `head_branch` | `text` |  |
| `event` | `text` |  |
| `status` | `text` |  |
| `conclusion` | `text` |  |
| `run_number` | `integer` |  |
| `run_attempt` | `integer` |  |
| `created_at` | `timestamp` |  |
| `updated_at` | `timestamp` |  |
| `run_started_at` | `timestamp` |  |

### `gh_workflows`

| Column | Type | |
|---|---|---|
| `id` | `bigint` | **PK** |
| `repo_id` | `text` | not null |
| `name` | `text` |  |
| `path` | `text` |  |
| `state` | `text` |  |
